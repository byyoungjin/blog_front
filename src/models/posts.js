export default class Posts {
  constructor(url, title, subTitle, contents, date) {
    this.url = url;
    this.title = title;
    this.subTitle = subTitle;
    this.contents = contents;
    this.date = date;
  }
}
