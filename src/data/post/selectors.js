export const getEditorTitleState = state =>
  state.post.currentPost.editorTitleState;
export const getEditorContentState = state =>
  state.post.currentPost.editorContentState;

export const getPosts = state => state.post.posts;
